# build output
dist/
# generated types
.astro/
# wrangler
.wrangler/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production
# Secrets locaux de `wrangler dev` — ce fichier n'existe pas aujourd'hui, et
# c'est précisément quand il apparaîtra (premier test local d'un endpoint) que
# rien ne l'aurait retenu : ce dépôt est mirroré en public sur GitHub.
.dev.vars
.dev.vars.*

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Generated Open Graph cards (rebuilt by scripts/og/gen-og.mjs at build)
public/og/

# Local terminal recordings (demo gif/webm) — deliberately kept out of the repo.
# A recording freezes a point-in-time state and then goes stale in silence: every
# other figure on this site is live from the KV, and the freshness guard only
# checks text, never pixels. Archive them outside the repo instead.
recordings/
