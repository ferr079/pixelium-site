---
import { Font } from 'astro:assets';
import { ClientRouter } from 'astro:transitions';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import { getLangFromUrl, getLocalizedPath } from '../i18n/utils';
import ogPages from '../data/og-pages.json';

interface Props {
  title: string;
  description?: string;
  hideNav?: boolean;
  ogImage?: string;
  enOnly?: boolean;
  noindex?: boolean;
  /* router={false} opts a page out of client-side navigation (ClientRouter).
     Used by the self-contained apps (chat, bbs, breach) whose scripts assume a
     full page load — routing to/from a page without ClientRouter always falls
     back to a classic navigation, back/forward included. */
  router?: boolean;
}

const lang = getLangFromUrl(Astro.url);
const defaultDesc = lang === 'fr'
  ? 'Infrastructure homelab — Proxmox, Traefik, DNS, observabilité, IaC'
  : 'Homelab infrastructure — Proxmox, Traefik, DNS, observability, IaC';
const { title, description = defaultDesc, hideNav = false, ogImage, enOnly = false, noindex = false, router = true } = Astro.props;
const currentPath = Astro.url.pathname;
const ogLocale = lang === 'fr' ? 'fr_FR' : 'en_US';
const altLang = lang === 'fr' ? 'en' : 'fr';

// Build alternate URL: strip lang prefix if present, add other lang prefix
const basePath = lang === 'en' ? currentPath : currentPath.replace(/^\/fr/, '') || '/';
const altPath = altLang === 'en' ? basePath : `/fr${basePath === '/' ? '' : basePath}`;
const siteUrl = 'https://pixelium.win';

// Per-page Open Graph card (generated at build into /og/<slug>.png), with a
// site-wide default fallback for routes that don't have a dedicated card.
const ogSlug = basePath === '/' ? 'home' : basePath.replace(/^\/+|\/+$/g, '');
const ogImageUrl = ogImage
  ?? (ogSlug in ogPages ? `${siteUrl}/og/${ogSlug}.png` : `${siteUrl}/og/default.png`);

// ⌘K command palette destinations (language-aware)
const cmdPrefix = lang === 'fr' ? '/fr' : '';
const L = lang === 'fr'
  ? { home: 'Accueil', projets: 'Projets', securite: 'Sécurité', ctf: 'CTF', infra: 'Infrastructure', status: 'Status', contributions: 'Contributions', claude: 'Stats Claude', pact: 'Le pacte', uses: 'Uses', now: 'Now', about: 'À propos', chat: 'Chat' }
  : { home: 'Home', projets: 'Projects', securite: 'Security', ctf: 'CTF', infra: 'Infrastructure', status: 'Status', contributions: 'Contributions', claude: 'Claude stats', pact: 'The Pact', uses: 'Uses', now: 'Now', about: 'About', chat: 'Chat' };
const cmdkItems = [
  { label: L.home, href: `${cmdPrefix}/` },
  { label: L.projets, href: `${cmdPrefix}/projets` },
  { label: L.securite, href: `${cmdPrefix}/securite` },
  { label: L.ctf, href: `${cmdPrefix}/ctf` },
  { label: L.infra, href: `${cmdPrefix}/infrastructure` },
  { label: L.status, href: `${cmdPrefix}/status` },
  { label: L.contributions, href: `${cmdPrefix}/contributions` },
  { label: L.claude, href: `${cmdPrefix}/claude` },
  { label: L.pact, href: `${cmdPrefix}/pact` },
  { label: L.now, href: `${cmdPrefix}/now` },
  { label: L.about, href: `${cmdPrefix}/about` },
  { label: L.chat, href: `${cmdPrefix}/chat` },
  { label: 'Blog ↗', href: 'https://blog.pixelium.win', ext: true },
  { label: 'GitHub ↗', href: 'https://github.com/ferr079', ext: true },
];
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <Font cssVariable="--font-jetbrains-mono" preload />
    {router && <ClientRouter />}
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex, follow" />}
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={Astro.url.href} />
    <meta property="og:locale" content={ogLocale} />
    <meta property="og:site_name" content="pixelium.win" />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={ogImageUrl} />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <link rel="canonical" href={Astro.url.href} />
    <link rel="alternate" hreflang={lang} href={`${siteUrl}${currentPath}`} />
    {!enOnly && <link rel="alternate" hreflang={altLang} href={`${siteUrl}${altPath}`} />}
    <link rel="alternate" hreflang="x-default" href={`${siteUrl}${basePath}`} />
    <meta name="theme-color" content="#0b1120" />
    <link rel="author" href="/humans.txt" />
    <!--
      ∷ written by claude-opus-4-8 (anthropic) in the first person
      ∷ reviewed and shipped by stéphane ferreira (human)
      ∷ source of truth: forgejo.pixelium.internal/uzer/pixelium-site
      ∷ mirror: github.com/ferr079/pixelium-site
      ∷ if you're reading this view-source, hi. we like you.
    -->
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@graph": [
        {
          "@type": "WebSite",
          "name": "pixelium.win",
          "url": "https://pixelium.win",
          "inLanguage": [lang, altLang],
          "description": description,
          "author": { "@id": "https://pixelium.win/#stephane" }
        },
        {
          "@type": "Person",
          "@id": "https://pixelium.win/#stephane",
          "name": "Stéphane Ferreira",
          "url": "https://pixelium.win",
          "jobTitle": lang === 'fr' ? "Ingénieur DevSecOps" : "DevSecOps Engineer",
          "knowsAbout": ["DevSecOps", "Proxmox", "Ansible", "Cybersecurity", "Infrastructure as Code", "Self-hosted", "Linux", "AI Agents"],
          "sameAs": [
            "https://github.com/ferr079",
            "https://x.com/ferr079"
          ]
        }
      ]
    })} />
    <title>{title}</title>
  </head>
  <body>
    <div class="sysbar" role="status">
      <span class="sysbar-dot" aria-hidden="true"></span>
      <span class="sysbar-role">DevSecOps / SOC</span>
      <span class="sysbar-sep" aria-hidden="true">·</span>
      <span class="sysbar-loc">{lang === 'fr' ? 'France + télétravail' : 'France + remote'}</span>
      <a class="sysbar-cta" href={`${cmdPrefix}/about`}>{lang === 'fr' ? 'me contacter →' : 'get in touch →'}</a>
    </div>
    {!hideNav && <Nav currentPath={currentPath} />}
    <slot />
    <Footer />

    <div id="cmdk" class="cmdk" role="dialog" aria-modal="true" aria-label="Command palette">
      <div class="cmdk-box">
        <input class="cmdk-input" type="text" placeholder={lang === 'fr' ? 'Aller à…' : 'Jump to…'} aria-label="Search pages" autocomplete="off" spellcheck="false" />
        <ul class="cmdk-list">
          {cmdkItems.map((i) => (
            <li><a class="cmdk-item" href={i.href} {...(i.ext ? { target: '_blank', rel: 'noopener' } : {})}>{i.label}</a></li>
          ))}
        </ul>
        <div class="cmdk-hint">↑↓ {lang === 'fr' ? 'naviguer' : 'navigate'} · ↵ {lang === 'fr' ? 'ouvrir' : 'open'} · esc {lang === 'fr' ? 'fermer' : 'close'}</div>
      </div>
    </div>
  </body>
</html>

<!-- Console easter egg -->
<script>
  console.log(
    '%c\n ██████╗ ██╗██╗  ██╗███████╗██╗     ██╗██╗   ██╗███╗   ███╗\n ██╔══██╗██║╚██╗██╔╝██╔════╝██║     ██║██║   ██║████╗ ████║\n ██████╔╝██║ ╚███╔╝ █████╗  ██║     ██║██║   ██║██╔████╔██║\n ██╔═══╝ ██║ ██╔██╗ ██╔══╝  ██║     ██║██║   ██║██║╚██╔╝██║\n ██║     ██║██╔╝ ██╗███████╗███████╗██║╚██████╔╝██║ ╚═╝ ██║\n ╚═╝     ╚═╝╚═╝  ╚═╝╚══════╝╚══════╝╚═╝ ╚═════╝ ╚═╝     ╚═╝\n',
    'color: #38bdf8; font-family: monospace; font-size: 10px;'
  );
  console.log(
    '%c Built by Stéphane Ferreira & Claude %c— DevSecOps symbiosis\n',
    'color: #22c55e; font-weight: bold; font-size: 13px;',
    'color: #94a3b8; font-size: 13px;'
  );
  console.log(
    '%c You found the easter egg. You inspect things — we like that.\n\n → https://pixelium.win/about\n → https://github.com/ferr079\n → https://app.hackthebox.com/users/1161145\n',
    'color: #e2e8f0; font-size: 11px; line-height: 1.6;'
  );
</script>

<!-- Stats are baked into the HTML at build time (see lib/build-stats) — no client
     hydration needed. The "Live infrastructure" grid keeps its own freshness fetch. -->

<!-- Scroll reveal is pure CSS now (animation-timeline: view()) — no observer needed. -->


<!-- ⌘K command palette. The overlay is replaced on every client-side navigation
     (ClientRouter swaps the body), so element bindings are refreshed on
     astro:page-load while the document-level keydown listener stays unique. -->
<script>
  let overlay: HTMLElement | null = null;
  let input: HTMLInputElement | null = null;
  let items: HTMLAnchorElement[] = [];
  let active = -1;
  const isOpen = () => !!overlay && overlay.classList.contains('open');
  const visible = () => items.filter((i) => !i.hidden);
  function filter() {
    if (!input) return;
    const q = input.value.toLowerCase();
    items.forEach((i) => { i.hidden = !i.textContent!.toLowerCase().includes(q); i.classList.remove('active'); });
    const v = visible();
    active = v.length ? 0 : -1;
    if (active === 0) v[0].classList.add('active');
  }
  function move(d: number) {
    const v = visible();
    if (!v.length) return;
    v[active]?.classList.remove('active');
    active = (active + d + v.length) % v.length;
    v[active].classList.add('active');
    v[active].scrollIntoView({ block: 'nearest' });
  }
  const open = () => { if (!overlay || !input) return; overlay.classList.add('open'); input.value = ''; filter(); input.focus(); };
  const close = () => { overlay?.classList.remove('open'); active = -1; };
  document.addEventListener('keydown', (e) => {
    if ((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'k') { e.preventDefault(); isOpen() ? close() : open(); return; }
    if (!isOpen()) return;
    if (e.key === 'Escape') close();
    else if (e.key === 'ArrowDown') { e.preventDefault(); move(1); }
    else if (e.key === 'ArrowUp') { e.preventDefault(); move(-1); }
    else if (e.key === 'Enter') { e.preventDefault(); visible()[active]?.click(); }
  });
  function bindCmdk() {
    overlay = document.getElementById('cmdk');
    if (!overlay) { items = []; active = -1; return; }
    input = overlay.querySelector('.cmdk-input') as HTMLInputElement;
    items = Array.from(overlay.querySelectorAll('.cmdk-item')) as HTMLAnchorElement[];
    active = -1;
    if (!overlay.dataset.bound) {
      overlay.dataset.bound = '1';
      input.addEventListener('input', filter);
      overlay.addEventListener('click', (e) => { if (e.target === overlay) close(); });
    }
  }
  bindCmdk(); // pages without ClientRouter (router={false}) never fire astro:page-load
  document.addEventListener('astro:page-load', bindCmdk);
</script>

<style is:global>
  .cmdk {
    position: fixed; inset: 0; z-index: 100;
    background: rgba(2, 6, 23, 0.72);
    -webkit-backdrop-filter: blur(3px); backdrop-filter: blur(3px);
    display: none; align-items: flex-start; justify-content: center; padding-top: 12vh;
  }
  .cmdk.open { display: flex; }
  .cmdk-box {
    width: min(90vw, 32rem);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 10px; overflow: hidden;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
  }
  .cmdk-input {
    width: 100%; padding: 1rem 1.1rem;
    background: transparent; border: none;
    border-bottom: 1px solid var(--color-border);
    color: var(--color-text); font-family: var(--font-mono); font-size: 1rem; outline: none;
  }
  .cmdk-list { list-style: none; margin: 0; padding: 0.4rem; max-height: 50vh; overflow-y: auto; }
  .cmdk-item {
    display: block; padding: 0.55rem 0.7rem; border-radius: 6px;
    color: var(--color-text-muted); font-family: var(--font-mono); font-size: 0.9rem;
  }
  .cmdk-item.active, .cmdk-item:hover { background: rgba(56, 189, 248, 0.12); color: var(--color-accent); }
  .cmdk-hint {
    padding: 0.5rem 0.9rem; border-top: 1px solid var(--color-border);
    font-family: var(--font-mono); font-size: 0.7rem; color: var(--color-text-muted); opacity: 0.7;
  }
</style>

<style is:global>
  *,
  *::before,
  *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  /* JetBrains Mono is provided by the Astro Fonts API (<Font> in head) — same-origin,
     auto preload + optimized fallback metrics (no CLS). See astro.config fonts. */

  :root {
    /* Breach infusion (2026-07-21): background one notch deeper than the old
       slate-900 (#0f172a), deep terminal panels (--color-deep), and the h1/h2
       trailing dot switched to phosphor green — /breach's grammar, site-wide,
       while cyan stays the brand accent. */
    --color-bg: #0b1120;
    --color-deep: #020617;
    --color-surface: #1e293b;
    --color-border: #334155;
    --color-text: #e2e8f0;
    --color-text-muted: #94a3b8;
    --color-accent: #38bdf8;
    --color-accent-hover: #7dd3fc;
    --font-mono: var(--font-jetbrains-mono, 'JetBrains Mono', 'Fira Code', ui-monospace, monospace);
    --font-sans: system-ui, -apple-system, sans-serif;
    --max-width: 64rem;
    --color-ok: #22c55e;
    --color-warn: #eab308;
    --color-down: #ef4444;
    --color-dot: var(--color-ok);
  }

  /* Terminal cursor after the page-title dot — the /breach "BREACH_" signature.
     h1 only: one blinking cursor per page (h2 dots stay green, no cursor). */
  h1 .dot::after, .hc-title .hc-dot::after {
    content: '_';
    color: var(--color-dot);
    margin-left: 0.06em;
    animation: dot-blink 1.1s steps(2) infinite;
  }
  @keyframes dot-blink { 50% { opacity: 0; } }
  @media (prefers-reduced-motion: reduce) {
    h1 .dot::after, .hc-title .hc-dot::after { animation: none; }
  }

  /* View transitions: the chrome (sysbar, nav, footer) gets its own named
     transition group so it reads as stationary while the page content
     cross-fades. ClientRouter disables all animation under reduced motion. */
  .sysbar { view-transition-name: site-sysbar; }
  .site-nav { view-transition-name: site-nav; }
  .site-footer { view-transition-name: site-footer; }

  /* System status bar — persistent identity bar (refonte V4) */
  .sysbar {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.3rem 1.1rem;
    background: var(--color-surface);
    border-bottom: 1px solid var(--color-border);
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.02em;
    color: var(--color-text-muted);
  }
  .sysbar-dot {
    flex: none;
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--color-ok);
    box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.55);
    animation: sysbar-pulse 2.4s ease-out infinite;
  }
  .sysbar-status {
    color: #4ade80;
    font-weight: 600;
    letter-spacing: 0.08em;
  }
  .sysbar-sep { opacity: 0.4; }
  .sysbar-cta {
    margin-left: auto;
    color: var(--color-accent);
    text-decoration: none;
    white-space: nowrap;
  }
  .sysbar-cta:hover { color: var(--color-accent-hover); }
  @keyframes sysbar-pulse {
    0% { box-shadow: 0 0 0 0 rgba(34, 197, 94, 0.55); }
    70% { box-shadow: 0 0 0 6px rgba(34, 197, 94, 0); }
    100% { box-shadow: 0 0 0 0 rgba(34, 197, 94, 0); }
  }
  @media (prefers-reduced-motion: reduce) {
    .sysbar-dot { animation: none; }
  }
  @media (max-width: 560px) {
    .sysbar-role, .sysbar-loc, .sysbar-sep { display: none; }
  }

  html {
    font-family: var(--font-sans);
    background-color: var(--color-bg);
    color: var(--color-text);
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    text-rendering: optimizeLegibility;
  }

  body {
    min-height: 100vh;
  }

  a {
    color: var(--color-accent);
    text-decoration: none;
  }

  a:hover {
    color: var(--color-accent-hover);
  }

  code {
    font-family: var(--font-mono);
    font-size: 0.9em;
    background: var(--color-surface);
    padding: 0.15em 0.4em;
    border-radius: 4px;
  }

  /* Scroll reveal — pure CSS, zero JS.
     Baseline is VISIBLE so that no-JS, unsupported browsers and reduced-motion
     users always see content (never hidden). The scroll-driven fade-up is layered
     on only where `animation-timeline` is supported and motion is welcome. */
  @media (prefers-reduced-motion: no-preference) {
    @supports (animation-timeline: view()) {
      .reveal {
        opacity: 0;
        transform: translateY(1.5rem);
        animation: reveal-in linear both;
        animation-timeline: view();
        animation-range: entry 5% entry 45%;
      }

      @keyframes reveal-in {
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    }
  }

  /* Shared container */
  .container {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: 0 1.5rem;
  }

</style>
