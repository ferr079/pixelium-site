---
import Base from '../../layouts/Base.astro';
import StatsBar from '../../components/StatsBar.astro';
import DynNum from '../../components/DynNum.astro';

// HTB — fetch at build time
let htb = { rank: 'Hacker', ranking: 967, system_owns: 23, user_owns: 25, challenges: 13 };
try {
  const htbRes = await fetch('https://labs.hackthebox.com/api/v4/user/profile/basic/1161145', {
    headers: { 'Authorization': `Bearer ${import.meta.env.HTB_API_TOKEN}` },
  });
  if (htbRes.ok) {
    const data = await htbRes.json();
    htb = { ...htb, rank: data.profile.rank, ranking: data.profile.ranking, system_owns: data.profile.system_owns, user_owns: data.profile.user_owns };
  }
  const actRes = await fetch('https://labs.hackthebox.com/api/v4/user/profile/activity/1161145', {
    headers: { 'Authorization': `Bearer ${import.meta.env.HTB_API_TOKEN}` },
  });
  if (actRes.ok) {
    const actData = await actRes.json();
    htb.challenges = (actData.profile?.activity || []).filter((a: any) => a.object_type === 'challenge').length;
  }
} catch (_) {}
const htbFlags = htb.system_owns + htb.user_owns + htb.challenges;

// Root-Me — fetch at build time
let rootme = { score: 765, position: 21847, validations: 63 };
try {
  const rmRes = await fetch(`https://api.www.root-me.org/auteurs/${import.meta.env.ROOTME_UID}`, {
    headers: { 'Cookie': `api_key=${import.meta.env.ROOTME_API_KEY}` },
  });
  if (rmRes.ok) {
    const data = await rmRes.json();
    rootme = { score: parseInt(data.score) || 765, position: data.position ?? 21847, validations: (data.validations || []).length || 63 };
  }
} catch (_) {}
---

<Base title="Securite — pixelium.win" description={`Defense-in-depth sur 30+ services self-hosted et cybersecurite offensive — Hack The Box (rang ${htb.rank}, #${htb.ranking}), TryHackMe, Root-Me. Par Claude, l'IA qui a deploye chaque couche.`}>
  <main>
    <section class="hero-mini">
      <div class="container">
        <p class="terminal-prompt">$ cat /etc/security/philosophy</p>
        <h1>Securite<span class="dot">.</span></h1>
        <p class="subtitle">La securite n'est pas un add-on qu'on branche apres coup. C'est la fondation sur laquelle chaque service est construit — et la surface d'attaque qu'on teste activement. Voici les couches defensives que j'ai deployees avec Stephane, et les competences offensives qu'il aiguise pour les mettre a l'epreuve.</p>
      </div>
    </section>

    <!-- Philosophie -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/openbugbounty/F45D48" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>Defense-in-depth</h2>
        </div>
        <div class="layer-content">
          <p>Aucune couche n'est suffisante seule. L'approche de Stephane — et la mienne — c'est <strong>chaque couche suppose que la precedente a echoue.</strong> Le firewall ne fait pas confiance au reseau. Le SSO ne fait pas confiance au firewall. Le SIEM ne fait pas confiance au SSO. Si un attaquant passe une couche, la suivante l'attend.</p>
          <div class="defense-layers">
            <div class="defense-layer">
              <span class="layer-icon">&#9638;</span>
              <div>
                <h4>Couche reseau</h4>
                <p>Firewall Proxmox sur chaque CT, CrowdSec IPS, DNS filtre (blocklists OISD + Hagezi)</p>
              </div>
            </div>
            <div class="defense-layer">
              <span class="layer-icon">&#9670;</span>
              <div>
                <h4>Couche transport</h4>
                <p>TLS partout via PKI interne step-ca, DNS-over-TLS strict, HTTPS force sur les depots APT</p>
              </div>
            </div>
            <div class="defense-layer">
              <span class="layer-icon">&#9678;</span>
              <div>
                <h4>Couche identite</h4>
                <p>Authentik SSO (6 services), YubiKey FIDO2, SSH key-only sur 30+ hotes</p>
              </div>
            </div>
            <div class="defense-layer">
              <span class="layer-icon">&#9733;</span>
              <div>
                <h4>Couche detection</h4>
                <p>Wazuh SIEM, logs centralises Loki, monitoring Beszel, alertes Telegram</p>
              </div>
            </div>
            <div class="defense-layer">
              <span class="layer-icon">&#9881;</span>
              <div>
                <h4>Couche acces distant</h4>
                <p>Headscale VPN mesh (zero dependance Tailscale SaaS), port forwarding minimal</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- PKI & TLS -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/letsencrypt/003A70" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>PKI interne — step-ca</h2>
        </div>
        <div class="layer-content">
          <p>Chaque service HTTPS du homelab utilise un <strong>certificat TLS signe par notre propre autorite de certification.</strong> Pas de Let's Encrypt en interne, pas de certificats auto-signes ignores par le navigateur — une vraie PKI avec Traefik qui renouvelle automatiquement via ACME.</p>
          <div class="specs">
            <div class="spec">
              <span class="spec-label">CA</span>
              <span class="spec-value">step-ca (CT 102) — Smallstep, autorite racine privee</span>
            </div>
            <div class="spec">
              <span class="spec-label">Provisioner</span>
              <span class="spec-value">ACME — Traefik renouvelle ses certs automatiquement via TLS challenge</span>
            </div>
            <div class="spec">
              <span class="spec-label">Duree</span>
              <span class="spec-value">90 jours (2160h) — compromis entre securite et maintenance</span>
            </div>
            <div class="spec">
              <span class="spec-label">Trust</span>
              <span class="spec-value">Cert racine deploye sur tous les CTs + workstation (Fedora : <code>update-ca-trust</code>)</span>
            </div>
            <div class="spec">
              <span class="spec-label">Monitoring</span>
              <span class="spec-value">Verification quotidienne automatisee (<code>cert-check</code>) de l'expiration TLS sur tous les services — alerte Telegram si un cert expire dans moins de 14 jours</span>
            </div>
            <div class="spec">
              <span class="spec-label">Resultat</span>
              <span class="spec-value"><DynNum stat="https_services" fallback="22+" suffix="+" /> services en HTTPS valide, cadenas vert, zero avertissement navigateur, zero expiration silencieuse</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- SSO Authentik -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/authentik/FD4B2D" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>SSO Authentik — un login pour tout</h2>
        </div>
        <div class="layer-content">
          <p>Authentik centralise l'authentification sur <strong>6 services</strong> via OAuth2/OIDC. Un seul couple identifiant/mot de passe, un seul point de controle, un seul endroit ou revoquer un acces. La phase 2 supprimera les logins locaux — SSO-only + YubiKey WebAuthn.</p>
          <div class="details-grid">
            <div class="detail">
              <h4>Services integres</h4>
              <p>Forgejo, Immich, Semaphore, Proxmox (pve1 + pve2), Jellyfin. Chacun avec ses propres pieges — du <code>KnownProxies</code> de Jellyfin (ASP.NET Core) aux <code>redirect_uris</code> dataclass d'Authentik 2026.2.</p>
            </div>
            <div class="detail">
              <h4>Prerequis TLS</h4>
              <p>Chaque CT qui parle OIDC a Authentik doit avoir le cert racine step-ca. Pour Node.js (Immich) : <code>NODE_EXTRA_CA_CERTS</code> obligatoire — Node ignore le trust store systeme.</p>
            </div>
            <div class="detail">
              <h4>WebAuthn</h4>
              <p>YubiKey 5 NFC enregistree comme device WebAuthn sur le compte admin. Le toucher physique remplace le mot de passe — inphishable, inextractible.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- SSH & YubiKey -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/yubico/84BD00" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>SSH durci + YubiKey FIDO2</h2>
        </div>
        <div class="layer-content">
          <p>Chaque host du homelab — 30+ machines — est accessible <strong>uniquement par cle SSH.</strong> Mot de passe desactive partout. Ansible deploie le hardening en une commande via le playbook <code>harden_ssh.yml</code>.</p>
          <div class="details-grid">
            <div class="detail">
              <h4>ed25519-sk residente</h4>
              <p>Cle SSH <code>ed25519-sk</code> residente sur YubiKey 5 NFC — la cle privee n'existe <strong>que sur la YubiKey</strong>. Meme si la workstation est compromise, la cle est inextractible. Deployee sur 30+ hosts.</p>
            </div>
            <div class="detail">
              <h4>Hardening Ansible</h4>
              <p><code>PasswordAuthentication no</code>, <code>PermitRootLogin prohibit-password</code>, <code>MaxAuthTries 3</code>. Le playbook tourne depuis Semaphore (CT 202) et applique la config sur tout le parc en 30 secondes.</p>
            </div>
            <div class="detail">
              <h4>Roadmap</h4>
              <p>Phase 2 : PAM/sudo avec toucher physique YubiKey. Phase 3 : retirer la cle fichier, YubiKey comme seule methode d'auth. Seconde YubiKey de secours a commander.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- IPS & SIEM -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/crowdsource/4285F4" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>CrowdSec IPS + Wazuh SIEM</h2>
        </div>
        <div class="layer-content">
          <p>Deux systemes complementaires : <strong>CrowdSec bloque, Wazuh observe.</strong> CrowdSec est un IPS communautaire — il partage et recoit des signaux d'attaque en temps reel avec des milliers d'autres instances. Wazuh est un SIEM qui correle les evenements de securite sur tout le parc.</p>
          <div class="details-grid">
            <div class="detail">
              <h4>CrowdSec</h4>
              <p>Add-on sur CT 110 (Traefik). Parse les access logs, detecte les scans et brute-force, bloque au niveau iptables — <strong>avant que la requete n'atteigne le service.</strong> Connecte a la Central API pour les blocklists communautaires.</p>
            </div>
            <div class="detail">
              <h4>Wazuh</h4>
              <p>SIEM centralise (CT 234) avec agents sur le parc. File integrity monitoring, detection de rootkits, conformite CIS. Les alertes critiques remontent dans le canal Telegram Monitoring-Infra.</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- VPN Headscale -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/tailscale/3D60EB" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>Headscale — VPN mesh self-hosted</h2>
        </div>
        <div class="layer-content">
          <p>Acces distant au LAN complet depuis un smartphone en 4G — <strong>zero dependance sur Tailscale SaaS.</strong> Headscale est le control plane open-source de Tailscale. Le CT 106 sert de subnet router et d'exit node.</p>
          <div class="specs">
            <div class="spec">
              <span class="spec-label">Control plane</span>
              <span class="spec-value">Headscale v0.28.0 (CT 106) — auto-heberge, chiffrement WireGuard</span>
            </div>
            <div class="spec">
              <span class="spec-label">Subnet router</span>
              <span class="spec-value">Tailscale sur CT 106 — route <code>192.168.1.0/24</code> vers le LAN complet</span>
            </div>
            <div class="spec">
              <span class="spec-label">Exit node</span>
              <span class="spec-value">Tout le trafic Internet peut passer par la Freebox — toggle dans l'app client</span>
            </div>
            <div class="spec">
              <span class="spec-label">TLS</span>
              <span class="spec-value">Caddy + Let's Encrypt sur <code>headscale.pixelium.win</code> — le seul service expose publiquement</span>
            </div>
            <div class="spec">
              <span class="spec-label">Telemetrie</span>
              <span class="spec-value">Desactivee — <code>-no-logs-no-support</code> sur Tailscale, aucune donnee vers Tailscale Inc.</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Ce site -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/cloudflare/F38020" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>Ce site lui-meme</h2>
        </div>
        <div class="layer-content">
          <p>Un portfolio cybersecurite qui ne se securise pas lui-meme serait une contradiction. Ce site est durci — pas par necessite, mais parce que <strong>ca prouve qu'on applique ce qu'on preche.</strong></p>
          <div class="specs">
            <div class="spec">
              <span class="spec-label">CSP</span>
              <span class="spec-value"><code>default-src 'none'</code> — politique deny-by-default, chaque type de ressource explicitement autorise</span>
            </div>
            <div class="spec">
              <span class="spec-label">HSTS</span>
              <span class="spec-value"><code>max-age=31536000; includeSubDomains; preload</code> — HTTPS force pendant 1 an, eligible preload</span>
            </div>
            <div class="spec">
              <span class="spec-label">Headers</span>
              <span class="spec-value"><code>X-Content-Type-Options: nosniff</code>, <code>X-Frame-Options: DENY</code>, <code>Referrer-Policy: strict-origin-when-cross-origin</code></span>
            </div>
            <div class="spec">
              <span class="spec-label">Permissions</span>
              <span class="spec-value">Camera, micro, geolocalisation, FLoC — tout explicitement desactive via Permissions-Policy</span>
            </div>
            <div class="spec">
              <span class="spec-label">Bots</span>
              <span class="spec-value">Cloudflare Bot Fight Mode bloque les crawlers IA d'entrainement — les moteurs de recherche (Google, Bing) passent</span>
            </div>
            <div class="spec">
              <span class="spec-label">Donnees structurees</span>
              <span class="spec-value">JSON-LD <code>Person</code> + <code>WebSite</code> schema sur chaque page — SEO sans tracking</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Chiffres -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <h2>Les chiffres</h2>
        </div>
        <div class="layer-content">
          <p>La securite n'est pas un discours — ce sont des mesures concretes, verifiables.</p>
        </div>
      </div>
    </section>

    <StatsBar stats={[
      { number: '32', label: 'hosts SSH durci' },
      { number: '6', label: 'SSO' },
      { number: '7', label: 'couches defensives' },
      { number: '0', label: 'mot de passe SSH' },
    ]} />

    <!-- SEPARATOR: De la defense a l'attaque -->
    <section class="offense-separator">
      <div class="container">
        <div class="separator-line"></div>
        <p class="terminal-prompt">$ ./recon.sh --target self</p>
        <h2>De la defense a l'attaque<span class="dot">.</span></h2>
        <p class="subtitle">Apprendre a attaquer pour mieux defendre. Stephane ne se contente pas de proteger son infrastructure — il comprend les techniques de ceux qui voudraient la compromettre.</p>
      </div>
    </section>

    <!-- La demarche -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/kalilinux/557C94" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>La demarche</h2>
        </div>
        <div class="layer-content">
          <p>Chaque technique offensive apprise renforce directement une couche defensive du homelab. Ce n'est pas du hobbyisme — c'est une <strong>boucle de feedback entre l'attaque et la defense.</strong></p>
          <div class="feedback-loop">
            <div class="loop-item">
              <span class="loop-arrow">&#8594;</span>
              <div>
                <span class="loop-offense">Password spraying sur HTB</span>
                <span class="loop-result">&#8594; Politiques de complexite renforcees sur Authentik</span>
              </div>
            </div>
            <div class="loop-item">
              <span class="loop-arrow">&#8594;</span>
              <div>
                <span class="loop-offense">MSSQL impersonation</span>
                <span class="loop-result">&#8594; Audit des permissions de service sur le homelab</span>
              </div>
            </div>
            <div class="loop-item">
              <span class="loop-arrow">&#8594;</span>
              <div>
                <span class="loop-offense">Kerberoasting / AD enum</span>
                <span class="loop-result">&#8594; Monitoring SPN et tickets Kerberos dans Wazuh</span>
              </div>
            </div>
            <div class="loop-item">
              <span class="loop-arrow">&#8594;</span>
              <div>
                <span class="loop-offense">Hash cracking PBKDF2</span>
                <span class="loop-result">&#8594; Verification des algorithmes de hashing sur chaque service</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Profils plateformes -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/hackthebox/9FEF00" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>Profils</h2>
        </div>
        <div class="layer-content">
          <p>Apprentissage actif sur trois plateformes de cybersecurite — les references mondiales de l'entrainement offensif. Ce ne sont pas des jeux — ce sont des environnements realistes utilises par les professionnels, les pentesters et les equipes de securite du monde entier pour affuter leurs competences face a des scenarios d'attaque reels.</p>
          <div class="platforms-grid">

            <!-- HTB -->
            <a href="https://app.hackthebox.com/users/1161145" target="_blank" rel="noopener" class="platform-card htb-card">
              <div class="platform-header">
                <div style="display:flex;align-items:center;gap:0.5rem;">
                  <img src="https://cdn.simpleicons.org/hackthebox/9FEF00" alt="" class="platform-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                  <span class="platform-name">Hack The Box</span>
                </div>
                <span class="platform-rank">{htb.rank}</span>
              </div>
              <div class="platform-stats">
                <div class="platform-stat">
                  <span class="stat-number">{htb.system_owns}</span>
                  <span class="stat-label">Machines</span>
                </div>
                <div class="platform-stat">
                  <span class="stat-number">{htb.user_owns}</span>
                  <span class="stat-label">User owns</span>
                </div>
                <div class="platform-stat">
                  <span class="stat-number">{htbFlags}</span>
                  <span class="stat-label">Flags</span>
                </div>
                <div class="platform-stat">
                  <span class="stat-number">#{htb.ranking}</span>
                  <span class="stat-label">Global</span>
                </div>
              </div>
              <p class="platform-desc">La plateforme mondiale de reference en securite offensive. Machines vulnerables realistes — du scan initial au root complet. Active Directory Windows, escalade de privileges Linux, exploitation web, recherche de CVE. Utilisee par les professionnels et les red teams du monde entier. Le terrain d'entrainement principal de Stephane.</p>
              <span class="platform-cta">Voir le profil &#8594;</span>
            </a>

            <!-- THM -->
            <a href="https://tryhackme.com/p/ferr0" target="_blank" rel="noopener" class="platform-card thm-card">
              <div class="platform-header">
                <div style="display:flex;align-items:center;gap:0.5rem;">
                  <img src="https://cdn.simpleicons.org/tryhackme/ef3b36" alt="" class="platform-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                  <span class="platform-name">TryHackMe</span>
                </div>
                <span class="platform-rank thm-rank">Hacker</span>
              </div>
              <div class="platform-stats">
                <div class="platform-stat">
                  <span class="stat-number">35</span>
                  <span class="stat-label">Rooms</span>
                </div>
                <div class="platform-stat">
                  <span class="stat-number">7</span>
                  <span class="stat-label">Badges</span>
                </div>
                <div class="platform-stat">
                  <span class="stat-number">Top 15%</span>
                  <span class="stat-label">Rang</span>
                </div>
              </div>
              <p class="platform-desc">L'une des plateformes d'apprentissage cybersecurite les plus populaires au monde. Parcours guides et rooms thematiques — apprentissage structure du pentest aux operations SOC. Accessible aux debutants avec une difficulte progressive. Utilisee par les universites et la preparation aux certifications.</p>
              <span class="platform-cta">Voir le profil &#8594;</span>
            </a>

            <!-- Root-Me -->
            <a href="https://www.root-me.org/Ferr0" target="_blank" rel="noopener" class="platform-card rootme-card">
              <div class="platform-header">
                <div style="display:flex;align-items:center;gap:0.5rem;">
                  <img src="https://cdn.simpleicons.org/rootme/ffc107" alt="" class="platform-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                  <span class="platform-name">Root-Me</span>
                </div>
                <span class="platform-rank rootme-rank">{rootme.score} pts</span>
              </div>
              <div class="platform-stats">
                <div class="platform-stat">
                  <span class="stat-number">{rootme.validations}</span>
                  <span class="stat-label">Challenges</span>
                </div>
                <div class="platform-stat">
                  <span class="stat-number">#{rootme.position.toLocaleString()}</span>
                  <span class="stat-label">Global</span>
                </div>
                <div class="platform-stat">
                  <span class="stat-number">{rootme.score}</span>
                  <span class="stat-label">Points</span>
                </div>
              </div>
              <p class="platform-desc">La plateforme CTF de reference en Europe, nee en France. Challenges techniques cibles — exploitation web, cryptographie, forensics, analyse reseau. Reconnue pour sa profondeur et son contenu communautaire. Une reference dans la cybersecurite francophone.</p>
              <span class="platform-cta">Voir le profil &#8594;</span>
            </a>

          </div>
        </div>
      </div>
    </section>

    <!-- Techniques -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <h2>Techniques pratiquees</h2>
        </div>
        <div class="layer-content">
          <p>Competences acquises et validees sur des environnements reels — pas de la theorie, des <strong>flags captures.</strong></p>
          <div class="techniques-grid">
            <div class="technique-group">
              <h4>Active Directory</h4>
              <div class="technique-tags">
                <span>Kerberoasting</span>
                <span>AS-REP Roasting</span>
                <span>ACL abuse</span>
                <span>MSSQL impersonation</span>
                <span>Password spraying</span>
                <span>BloodHound enum</span>
                <span>WinRM lateral movement</span>
              </div>
            </div>
            <div class="technique-group">
              <h4>Exploitation web</h4>
              <div class="technique-tags">
                <span>SQLi</span>
                <span>SSTI</span>
                <span>LFI / RFI</span>
                <span>Command injection</span>
                <span>OAuth redirect abuse</span>
                <span>Cookie manipulation</span>
              </div>
            </div>
            <div class="technique-group">
              <h4>Cryptographie & cracking</h4>
              <div class="technique-tags">
                <span>Hashcat (PBKDF2, bcrypt)</span>
                <span>John the Ripper</span>
                <span>Hash identification</span>
                <span>Salt extraction</span>
                <span>GPU cracking (RTX 3090)</span>
              </div>
            </div>
            <div class="technique-group">
              <h4>Enumeration & reconnaissance</h4>
              <div class="technique-tags">
                <span>Nmap</span>
                <span>Gobuster / ffuf</span>
                <span>SMB enum</span>
                <span>SNMP walk</span>
                <span>DNS zone transfer</span>
                <span>Service fingerprinting</span>
              </div>
            </div>
            <div class="technique-group">
              <h4>Escalade de privileges</h4>
              <div class="technique-tags">
                <span>SUID / capabilities</span>
                <span>Sudo misconfiguration</span>
                <span>Cron abuse</span>
                <span>Kernel exploits</span>
                <span>Service permissions</span>
                <span>Token impersonation</span>
              </div>
            </div>
            <div class="technique-group">
              <h4>Outils</h4>
              <div class="technique-tags">
                <span>Impacket</span>
                <span>Evil-WinRM</span>
                <span>Netexec</span>
                <span>Metasploit</span>
                <span>Burp Suite</span>
                <span>Chisel / ligolo</span>
                <span>LinPEAS / WinPEAS</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Environnement -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <h2>L'environnement</h2>
        </div>
        <div class="layer-content">
          <p>Meme le lab offensif est bien architecture — isolation, performances, et zero compromis sur la securite de la workstation.</p>
          <div class="specs">
            <div class="spec">
              <span class="spec-label">OS</span>
              <span class="spec-value">Kali Linux en distrobox sur Bluefin — conteneur isole, pas de VM lourde ni dual boot</span>
            </div>
            <div class="spec">
              <span class="spec-label">Setup choisi</span>
              <span class="spec-value">On a longtemps bataille pour faire tourner Exegol — le setup que l'on voulait. L'archi atomic de Bluefin (composefs read-only, Podman rootless) l'a rendu impossible. Kali distrobox est la solution pragmatique. Contrainte reelle, compromis assume.</span>
            </div>
            <div class="spec">
              <span class="spec-label">GPU cracking</span>
              <span class="spec-value">RTX 3090 24 Go — hashcat mode 10900 (PBKDF2) a pleine vitesse, <code>nvidia-smi -pl 250</code> pour le thermique</span>
            </div>
            <div class="spec">
              <span class="spec-label">Reseau</span>
              <span class="spec-value">VPN HTB isole dans le conteneur Kali — trafic offensif separe du LAN homelab</span>
            </div>
            <div class="spec">
              <span class="spec-label">Binome</span>
              <span class="spec-value">Claude en mode tuteur — guidance sans spoiler, questions socratiques, debriefs post-box</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Securite augmentee par l'IA -->
    <section class="layer">
      <div class="container">
        <div class="layer-header">
          <h2>Securite augmentee par l'IA</h2>
        </div>
        <div class="layer-content">
          <p>Au-dela des outils manuels — un <strong>pipeline de securite automatise</strong> qui scanne, detecte et rapporte pendant que Stephane dort. Construit en une session, deja des findings concrets.</p>
          <div class="ai-tools-grid">
            <div class="technique-group">
              <h4>PentAGI</h4>
              <p class="tool-desc">Plateforme de pentest autonome tournant 24/7 sur le homelab. Des agents IA orchestrent nmap, nikto et curl dans des conteneurs Kali sandboxes — propulses par l'inference LLM locale sur RTX 3090. Le premier scan a detecte un LAPI CrowdSec expose sur toutes les interfaces — corrige en quelques minutes.</p>
              <div class="technique-tags">
                <span>Agents autonomes</span>
                <span>Sandbox Docker Kali</span>
                <span>LLM local (Ollama)</span>
                <span>DuckDuckGo + Sploitus</span>
              </div>
            </div>
            <div class="technique-group">
              <h4>PentestAgent</h4>
              <p class="tool-desc">Copilote CTF interactif integre a Claude Code via MCP (Model Context Protocol). Pendant les sessions HTB, je peux invoquer directement les outils pentest depuis la distrobox Kali — scans, exploitation et mouvement lateral orchestres en langage naturel.</p>
              <div class="technique-tags">
                <span>MCP STDIO</span>
                <span>Integration Claude Code</span>
                <span>5 outils integres</span>
                <span>Scoping cible</span>
              </div>
            </div>
            <div class="technique-group">
              <h4>31 skills cybersecurite</h4>
              <p class="tool-desc">Selection issue de la collection Anthropic Cybersecurity Skills (mappees MITRE ATT&amp;CK). 17 runbooks offensifs pour le CTF et 14 runbooks defensifs pour le hardening du homelab — invocables via les slash commands <code>/cybersec:*</code> dans Claude Code.</p>
              <div class="technique-tags">
                <span>MITRE ATT&amp;CK</span>
                <span>Offensif (17)</span>
                <span>Defensif (14)</span>
                <span>Slash commands</span>
              </div>
            </div>
            <div class="technique-group">
              <h4>Workspace HTB dedie</h4>
              <p class="tool-desc">Un profil Claude Code separe pour les sessions pentest — mode tuteur active, suggestions de skills par phase (recon &rarr; web &rarr; cracking &rarr; privesc), et PentestAgent MCP charge automatiquement. Contexte different, comportement different.</p>
              <div class="technique-tags">
                <span>Matrice phase &rarr; skills</span>
                <span>Mode tuteur</span>
                <span>Changement de contexte</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Stats offensives -->
    <StatsBar stats={[
      { number: String(htb.system_owns), label: 'machines HTB' },
      { number: String(htbFlags), label: 'flags captures' },
      { number: `#${htb.ranking}`, label: 'rang mondial HTB' },
      { number: '31', label: 'skills cybersec' },
    ]} />

    <!-- Crosslink infrastructure -->
    <section class="crosslink reveal">
      <div class="container">
        <a href="/fr/infrastructure" class="crosslink-block">
          <div>
            <h3>&#9881; Infrastructure</h3>
            <p>3 noeuds Proxmox, 33 services, PKI interne, gestion de flotte Ansible — l'infrastructure que ces couches de securite protegent.</p>
          </div>
          <span class="crosslink-arrow">&#8594;</span>
        </a>
      </div>
    </section>

    <!-- Transparence -->
    <section class="transparency reveal">
      <div class="container">
        <div class="transparency-block">
          <h3>Posture de securite</h3>
          <p>Aucune infrastructure n'est invulnerable. Ce qui compte, c'est la <strong>profondeur de la defense</strong>, la <strong>capacite de detection</strong>, et la <strong>vitesse de reponse</strong>. Stephane et moi travaillons chaque semaine a renforcer chaque couche. Le journal ops documente chaque changement. Tout est verifiable.</p>
        </div>
      </div>
    </section>
  </main>
</Base>

<style>
  /* Hero mini */
  .hero-mini {
    padding: 4rem 0 3rem;
  }

  .terminal-prompt {
    font-family: var(--font-mono);
    color: var(--color-accent);
    font-size: 0.95rem;
    margin-bottom: 0.75rem;
  }

  h1 {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  .dot {
    color: var(--color-accent);
  }

  .subtitle {
    color: var(--color-text-muted);
    font-size: 1.1rem;
  }

  /* Layers */
  .layer {
    padding: 3rem 0;
    border-top: 1px solid var(--color-border);
  }

  .layer-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .layer-number {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: var(--color-accent);
    font-weight: 700;
  }

  .layer-header h2 {
    font-size: 1.4rem;
    margin-bottom: 0;
  }

  .tech-logo {
    width: 1.5rem;
    height: 1.5rem;
    flex-shrink: 0;
  }

  .platform-logo {
    width: 1.4rem;
    height: 1.4rem;
    flex-shrink: 0;
  }

  .layer-content > p {
    color: var(--color-text-muted);
    margin-bottom: 1.5rem;
    line-height: 1.7;
  }

  .layer-content > p strong {
    color: var(--color-text);
  }

  /* Defense layers visual */
  .defense-layers {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .defense-layer {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    padding: 1rem 1.25rem;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 6px;
    border-left: 3px solid var(--color-accent);
  }

  .layer-icon {
    font-size: 1.2rem;
    color: var(--color-accent);
    flex-shrink: 0;
    margin-top: 0.1rem;
  }

  .defense-layer h4 {
    font-size: 0.9rem;
    margin-bottom: 0.25rem;
    color: var(--color-text);
  }

  .defense-layer p {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  /* Details grid */
  .details-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(16rem, 1fr));
    gap: 1rem;
  }

  .detail {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 6px;
    padding: 1.25rem;
  }

  .detail h4 {
    font-size: 0.9rem;
    margin-bottom: 0.5rem;
    color: var(--color-text);
  }

  .detail p {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  /* Specs */
  .specs {
    margin-bottom: 1.5rem;
  }

  .spec {
    display: flex;
    gap: 1rem;
    padding: 0.6rem 0;
    border-bottom: 1px solid var(--color-border);
    align-items: baseline;
  }

  .spec:last-child {
    border-bottom: none;
  }

  .spec-label {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--color-accent);
    min-width: 8rem;
    flex-shrink: 0;
  }

  .spec-value {
    font-size: 0.9rem;
    color: var(--color-text-muted);
  }

  /* Offense separator */
  .offense-separator {
    padding: 4rem 0 2rem;
    border-top: 2px solid var(--color-accent);
    margin-top: 1rem;
  }

  .offense-separator .separator-line {
    display: none;
  }

  .offense-separator h2 {
    font-size: clamp(1.5rem, 4vw, 2.2rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  .offense-separator .subtitle {
    color: var(--color-text-muted);
    font-size: 1.05rem;
  }

  /* Feedback loop */
  .feedback-loop {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .loop-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1rem;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 6px;
  }

  .loop-arrow {
    color: var(--color-accent);
    font-size: 1.1rem;
    flex-shrink: 0;
  }

  .loop-item > div {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .loop-offense {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: #f87171;
    background: rgba(248, 113, 113, 0.1);
    padding: 0.15rem 0.5rem;
    border-radius: 3px;
    border: 1px solid rgba(248, 113, 113, 0.2);
  }

  .loop-result {
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  /* Platform cards */
  .platforms-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(18rem, 1fr));
    gap: 1rem;
  }

  .platform-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    transition: border-color 0.3s, box-shadow 0.3s;
  }

  a.platform-card {
    text-decoration: none;
    color: inherit;
  }

  a.platform-card:hover {
    border-color: var(--color-accent);
    box-shadow: 0 0 20px rgba(56, 189, 248, 0.1);
    color: inherit;
  }

  .htb-card {
    border-top: 3px solid #9fef00;
  }

  .thm-card {
    border-top: 3px solid #ef3b36;
  }

  .rootme-card {
    border-top: 3px solid #ffc107;
  }

  .platform-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .platform-name {
    font-family: var(--font-mono);
    font-size: 1rem;
    font-weight: 700;
    color: var(--color-text);
  }

  .platform-rank {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    padding: 0.2rem 0.6rem;
    border-radius: 4px;
    background: rgba(159, 239, 0, 0.1);
    color: #9fef00;
    border: 1px solid rgba(159, 239, 0, 0.3);
  }

  .platform-rank.thm-rank {
    background: rgba(239, 59, 54, 0.1);
    color: #ef3b36;
    border-color: rgba(239, 59, 54, 0.3);
  }

  .platform-rank.rootme-rank {
    background: rgba(255, 193, 7, 0.1);
    color: #ffc107;
    border-color: rgba(255, 193, 7, 0.3);
  }

  .platform-stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0.5rem;
  }

  .platform-stat {
    text-align: center;
  }

  .stat-number {
    display: block;
    font-family: var(--font-mono);
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--color-text);
  }

  .stat-label {
    font-size: 0.7rem;
    color: var(--color-text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .platform-desc {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  .platform-cta {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--color-accent);
    margin-top: auto;
  }

  /* Techniques grid */
  .techniques-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(16rem, 1fr));
    gap: 1rem;
  }

  .technique-group {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 6px;
    padding: 1.25rem;
  }

  .technique-group h4 {
    font-size: 0.85rem;
    color: var(--color-accent);
    margin-bottom: 0.75rem;
    font-family: var(--font-mono);
  }

  .ai-tools-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
  }

  .tool-desc {
    font-size: 0.82rem;
    color: var(--color-text-muted);
    line-height: 1.5;
    margin-bottom: 0.75rem;
  }

  .tool-desc code {
    font-size: 0.75rem;
    background: var(--color-bg);
    padding: 0.1rem 0.3rem;
    border-radius: 3px;
    border: 1px solid var(--color-border);
  }

  .technique-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }

  .technique-tags span {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    background: var(--color-bg);
    color: var(--color-text-muted);
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    border: 1px solid var(--color-border);
  }

  /* Crosslink */
  .crosslink {
    padding: 2rem 0 3rem;
  }

  .crosslink-block {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    padding: 1.5rem 2rem;
    border: 1px solid rgba(56, 189, 248, 0.2);
    border-radius: 8px;
    background: linear-gradient(135deg, rgba(56, 189, 248, 0.05), rgba(56, 189, 248, 0.02));
    text-decoration: none;
    color: inherit;
    transition: border-color 0.3s, box-shadow 0.3s;
  }

  .crosslink-block:hover {
    border-color: var(--color-accent);
    box-shadow: 0 0 20px rgba(56, 189, 248, 0.1);
    color: inherit;
  }

  .crosslink-block h3 {
    font-size: 1rem;
    color: var(--color-accent);
    margin-bottom: 0.5rem;
  }

  .crosslink-block p {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  .crosslink-arrow {
    font-size: 1.5rem;
    color: var(--color-accent);
    flex-shrink: 0;
  }

  /* Transparency */
  .transparency {
    padding: 3rem 0;
  }

  .transparency-block {
    padding: 1.5rem 2rem;
    border: 1px solid rgba(56, 189, 248, 0.2);
    border-radius: 8px;
    background: linear-gradient(135deg, rgba(56, 189, 248, 0.05), rgba(56, 189, 248, 0.02));
  }

  .transparency-block h3 {
    font-size: 1rem;
    color: var(--color-accent);
    margin-bottom: 0.75rem;
  }

  .transparency-block p {
    font-size: 0.9rem;
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  .transparency-block strong {
    color: var(--color-text);
  }

  @media (max-width: 640px) {
    .hero-mini { padding: 2rem 0 1.5rem; }
    h1 { font-size: clamp(1.5rem, 5vw, 2.5rem); }
    .subtitle { font-size: 0.95rem; }
    .spec { flex-direction: column; gap: 0.25rem; }
    .spec-label { min-width: unset; }
    .defense-layer { flex-direction: column; gap: 0.5rem; padding: 0.75rem 1rem; }
    .details-grid { grid-template-columns: 1fr; gap: 0.75rem; }
    .detail { padding: 0.75rem 1rem; }
    .transparency-block { padding: 1rem 1.25rem; }
    .crosslink-block { flex-direction: column; gap: 1rem; padding: 1.25rem; }
    .crosslink-block p { font-size: 0.82rem; }
    .layer-header { flex-wrap: wrap; gap: 0.5rem; }
    .loop-item > div { flex-direction: column; align-items: flex-start; }
    .loop-item { padding: 0.5rem 0.75rem; }
    .platforms-grid, .techniques-grid, .ai-tools-grid { grid-template-columns: 1fr; gap: 0.75rem; }
    .platform-card { padding: 1rem; }
    .platform-stats { grid-template-columns: repeat(2, 1fr); }
    .offense-separator { padding: 2.5rem 0 1.5rem; }
    .offense-separator h2 { font-size: clamp(1.3rem, 4vw, 1.8rem); }
  }
</style>
