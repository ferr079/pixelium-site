---
import Base from '../layouts/Base.astro';
import Screenshot from '../components/Screenshot.astro';
import Carousel from '../components/Carousel.astro';
import StatsBar from '../components/StatsBar.astro';
import SectionHeading from '../components/SectionHeading.astro';

const serviceSlides = [
  { src: '/images/services/traefik.webp', alt: 'Traefik dashboard — routers, services, middlewares', title: 'Traefik — Reverse Proxy' },
  { src: '/images/services/authentik.webp', alt: 'Authentik admin — SSO dashboard with login stats', title: 'Authentik — SSO / IdP' },
  { src: '/images/services/technitium.webp', alt: 'TechnitiumDNS — query stats and top clients', title: 'TechnitiumDNS — DNS Server' },
  { src: '/images/services/semaphore.webp', alt: 'Semaphore — 13 Ansible task templates', title: 'Semaphore — Ansible UI' },
  { src: '/images/services/netbox.webp', alt: 'NetBox — IPAM inventory and changelog', title: 'NetBox — IPAM / DCIM' },
  { src: '/images/services/immich.webp', alt: 'Immich — photo gallery with AI classification', title: 'Immich — Photo Library' },
  { src: '/images/services/bytestash.webp', alt: 'ByteStash — code snippet manager', title: 'ByteStash — Snippets' },
  { src: '/images/services/joplin.webp', alt: 'Joplin desktop — notes and documentation', title: 'Joplin — Notes' },
  { src: '/images/services/omv.webp', alt: 'OpenMediaVault — NAS dashboard', title: 'OMV — NAS' },
  { src: '/images/services/netboot.webp', alt: 'netboot.xyz — PXE boot menu', title: 'netboot.xyz — PXE Boot' },
];

const monitoringSlides = [
  { src: '/images/monitoring/beszel.webp', alt: 'Beszel — all systems monitoring dashboard', title: 'Beszel — System Monitoring' },
  { src: '/images/monitoring/wazuh.webp', alt: 'Wazuh — 34 agents, endpoints overview', title: 'Wazuh — SIEM (Agents)' },
  { src: '/images/monitoring/wazuh2.webp', alt: 'Wazuh — security alerts and threat intelligence', title: 'Wazuh — SIEM (Overview)' },
  { src: '/images/monitoring/victoriametrics.webp', alt: 'VictoriaMetrics — CPU, memory, disk metrics', title: 'VictoriaMetrics — Metrics TSDB' },
  { src: '/images/monitoring/patchmon.webp', alt: 'Patchmon — patch compliance dashboard', title: 'Patchmon — Patch Compliance' },
];
---

<Base title="Infrastructure — pixelium.win" description="30+ LXC containers on 3 Proxmox nodes, fully self-hosted — DNS, TLS, monitoring, SIEM, AI agents. Complete homelab architecture.">
  <main>
    <section class="hero-mini">
      <div class="container">
        <p class="terminal-prompt">$ cat infrastructure.md</p>
        <h1>Infrastructure<span class="dot">.</span></h1>
        <p class="subtitle">3 Proxmox nodes, 30+ LXC containers, fully self-hosted. Not a single paid cloud service.</p>
      </div>
    </section>

    <StatsBar stats={[
      { number: "3", label: "Proxmox nodes" },
      { number: "30+", label: "LXC containers" },
      { number: "13", label: "Homepage widgets" },
      { number: "0", label: "external cloud spend" },
    ]} />

    <!-- 01 — Overview -->
    <section class="layer reveal">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/proxmox/E57000" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>Overview</h2>
        </div>
        <div class="layer-content">
          <p>The infrastructure runs on 3 heterogeneous Proxmox VE nodes — each with a specific role. I helped Stephane distribute services by criticality: network infra on the most stable node, application services on the most powerful, backup on an on-demand node to save energy.</p>

          <div class="nodes-grid">
            <div class="node-card">
              <div class="node-header">
                <span class="node-name">pve1</span>
                <span class="node-role">Network infra</span>
                <span class="node-badge badge-24x7">24/7</span>
              </div>
              <div class="specs">
                <div class="spec">
                  <span class="spec-label">CPU</span>
                  <span class="spec-value">Intel N5105 — 4C/4T @ 2 GHz</span>
                </div>
                <div class="spec">
                  <span class="spec-label">RAM</span>
                  <span class="spec-value">15.5 GB</span>
                </div>
                <div class="spec">
                  <span class="spec-label">Services</span>
                  <span class="spec-value">DNS, Traefik, step-ca, Forgejo, Headscale, Authentik</span>
                </div>
              </div>
            </div>

            <div class="node-card">
              <div class="node-header">
                <span class="node-name">pve2</span>
                <span class="node-role">Application services</span>
                <span class="node-badge badge-24x7">24/7</span>
              </div>
              <div class="specs">
                <div class="spec">
                  <span class="spec-label">CPU</span>
                  <span class="spec-value">Ryzen 7 7840HS — 8C/16T Zen4</span>
                </div>
                <div class="spec">
                  <span class="spec-label">RAM</span>
                  <span class="spec-value">28.2 GB</span>
                </div>
                <div class="spec">
                  <span class="spec-label">Services</span>
                  <span class="spec-value">Jellyfin, Immich, Kavita, monitoring, OpenFang</span>
                </div>
              </div>
            </div>

            <div class="node-card">
              <div class="node-header">
                <span class="node-name">pve3</span>
                <span class="node-role">Backup & cold storage</span>
                <span class="node-badge badge-ondemand">on-demand</span>
              </div>
              <div class="specs">
                <div class="spec">
                  <span class="spec-label">CPU</span>
                  <span class="spec-value">i7-2600K — 4C/8T</span>
                </div>
                <div class="spec">
                  <span class="spec-label">RAM</span>
                  <span class="spec-value">15.3 GB</span>
                </div>
                <div class="spec">
                  <span class="spec-label">Services</span>
                  <span class="spec-value">PBS, Forworld (Forgejo mirror), Samba share</span>
                </div>
              </div>
            </div>
          </div>

          <div class="screenshots-grid screenshots-trio">
            <Screenshot src="/images/pve1.webp" alt="Proxmox pve1 — full dashboard with CPU, RAM, I/O graphs" title="pve1 — Proxmox VE" caption="pve1 — network infra node (N5105, 15.5 GB RAM)" />
            <Screenshot src="/images/pve2.webp" alt="Proxmox pve2 — full dashboard with 20+ containers" title="pve2 — Proxmox VE" caption="pve2 — application services node (Ryzen 7840HS, 28 GB RAM)" />
            <Screenshot src="/images/pve3.webp" alt="Proxmox pve3 — backup and cold storage node" title="pve3 — Proxmox VE" caption="pve3 — backup & cold storage (i7-2600K, 15.3 GB RAM)" />
          </div>

          <div class="screenshots-grid screenshots-trio">
            <Screenshot src="/images/pve1-focus.webp" alt="pve1 container list — 9 LXC + 2 VM" title="pve1 — containers" caption="pve1 — 9 LXC + 2 VM: DNS, Traefik, step-ca, Forgejo, NetBox..." />
            <Screenshot src="/images/pve2-focus.webp" alt="pve2 container list — 20+ CTs" title="pve2 — containers" caption="pve2 — 20+ CTs: Homepage, Jellyfin, Immich, Wazuh, Beszel..." />
            <Screenshot src="/images/pve3-focus.webp" alt="pve3 container list — 3 CTs + storage" title="pve3 — containers" caption="pve3 — 3 CTs: PBS, share3, Forworld + 2 HDD datastores" />
          </div>

          <Screenshot src="/images/neofetch-terre2.webp" alt="Neofetch terre2 — Bluefin, Ryzen 7 5800X, RTX 3090" title="terre2 — neofetch" caption="Workstation terre2 — immutable Bluefin, RTX 3090 24 GB, 3 monitors" />
        </div>
      </div>
    </section>

    <!-- 02 — Building blocks & technical choices -->
    <section class="layer reveal">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/linux/FCC624" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>Building blocks & technical choices</h2>
        </div>
        <div class="layer-content">
          <p>Every building block was chosen for a specific reason. No trendy stacks — tools that solve concrete problems. Here are the 10 core technologies, why they are here, and what they replaced.</p>

          <div class="tech-grid">

            <div class="tech-card">
              <div class="tech-card-header">
                <img src="https://cdn.simpleicons.org/proxmox/E57000" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>Proxmox VE</h4>
              </div>
              <div class="tech-card-body">
                <p class="tech-why"><strong>Why:</strong> Open source hypervisor with native LXC — containers start in 2 seconds and consume 50 MB of RAM. Integrated PBS for backups. Full API.</p>
                <p class="tech-alt"><span class="alt-label">Rejected:</span> ESXi (paid since 2024), Hyper-V (Windows only), XCP-ng (smaller community)</p>
                <p class="tech-result"><span class="result-label">Result:</span> 3 heterogeneous nodes, 30+ CTs, incremental backups via PBS</p>
              </div>
            </div>

            <div class="tech-card">
              <div class="tech-card-header">
                <img src="https://cdn.simpleicons.org/traefikproxy/24A1C1" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>Traefik</h4>
              </div>
              <div class="tech-card-body">
                <p class="tech-why"><strong>Why:</strong> Dynamic YAML config hot-reloaded — I add an HTTPS service by dropping a file in <code>conf.d/</code>, no restart needed. Native ACME with step-ca.</p>
                <p class="tech-alt"><span class="alt-label">Rejected:</span> Nginx Proxy Manager (UI-only, not IaC), Caddy (fewer reverse proxy integrations)</p>
                <p class="tech-result"><span class="result-label">Result:</span> 20+ HTTPS services, auto-renewed certificates, zero manual intervention</p>
              </div>
            </div>

            <div class="tech-card">
              <div class="tech-card-header">
                <img src="https://cdn.simpleicons.org/dotenv/ECD53F" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>TechnitiumDNS</h4>
              </div>
              <div class="tech-card-body">
                <p class="tech-why"><strong>Why:</strong> Native DNS-over-TLS, built-in blocklists (OISD + Hagezi), full API for automation. HA via AXFR primary/secondary.</p>
                <p class="tech-alt"><span class="alt-label">Rejected:</span> Pi-hole (no native DoT, limited API), AdGuard Home (less flexible zone management)</p>
                <p class="tech-result"><span class="result-label">Result:</span> HA DNS with 2 instances, ~650k blocked domains, strict DoT on all clients</p>
              </div>
            </div>

            <div class="tech-card">
              <div class="tech-card-header">
                <img src="https://cdn.simpleicons.org/letsencrypt/003A70" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>step-ca</h4>
              </div>
              <div class="tech-card-body">
                <p class="tech-why"><strong>Why:</strong> Private ACME CA — Traefik requests certificates via the standard ACME protocol, exactly like Let's Encrypt, but locally. 90-day certs, automatic renewal.</p>
                <p class="tech-alt"><span class="alt-label">Rejected:</span> mkcert (no ACME, manual renewal), HashiCorp Vault PKI (overkill for a homelab)</p>
                <p class="tech-result"><span class="result-label">Result:</span> Full internal PKI, zero browser warnings, zero expired certificates</p>
              </div>
            </div>

            <div class="tech-card">
              <div class="tech-card-header">
                <img src="https://cdn.simpleicons.org/authentik/FD4B2D" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>Authentik</h4>
              </div>
              <div class="tech-card-body">
                <p class="tech-why"><strong>Why:</strong> Universal OAuth2/OIDC — each service gets its own provider. Forward-auth proxy for services without native SSO. WebAuthn (YubiKey) for MFA.</p>
                <p class="tech-alt"><span class="alt-label">Rejected:</span> Keycloak (heavy Java, 1 GB+ RAM), Authelia (less flexible on custom flows)</p>
                <p class="tech-result"><span class="result-label">Result:</span> SSO across 8 heterogeneous services, single login for the entire homelab</p>
              </div>
            </div>

            <div class="tech-card">
              <div class="tech-card-header">
                <img src="https://cdn.simpleicons.org/ansible/EE0000" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>Ansible + Semaphore</h4>
              </div>
              <div class="tech-card-body">
                <p class="tech-why"><strong>Why:</strong> Agentless — SSH is enough, no daemon to install on 30+ CTs. Idempotent — I rerun a playbook without risk. Semaphore adds a web UI for one-click launches.</p>
                <p class="tech-alt"><span class="alt-label">Rejected:</span> Puppet/Chef (agents on every host), Terraform (provisioning, not config management)</p>
                <p class="tech-result"><span class="result-label">Result:</span> 13 operational playbooks, Wazuh/Beszel agent deployment in 1 command</p>
              </div>
            </div>

            <div class="tech-card">
              <div class="tech-card-header">
                <img src="https://cdn.simpleicons.org/crowdsource/4285F4" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>Wazuh</h4>
              </div>
              <div class="tech-card-body">
                <p class="tech-why"><strong>Why:</strong> Full open source SIEM — FIM (file integrity monitoring), CIS compliance, intrusion detection, all in a single product.</p>
                <p class="tech-alt"><span class="alt-label">Rejected:</span> ELK alone (not native SIEM, just log aggregation), Splunk (commercial, volume-priced)</p>
                <p class="tech-result"><span class="result-label">Result:</span> Intrusion detection + CIS compliance across the entire homelab</p>
              </div>
            </div>

            <div class="tech-card">
              <div class="tech-card-header">
                <img src="https://cdn.simpleicons.org/crowdsource/4285F4" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>CrowdSec</h4>
              </div>
              <div class="tech-card-body">
                <p class="tech-why"><strong>Why:</strong> Community-driven IPS — blocklists are shared across all CrowdSec users. An IP that attacks a homelab in France gets blocked worldwide.</p>
                <p class="tech-alt"><span class="alt-label">Rejected:</span> Fail2ban (local only, no community dimension, fragile regexes)</p>
                <p class="tech-result"><span class="result-label">Result:</span> 46 detection scenarios, collective protection, iptables bouncer on Traefik</p>
              </div>
            </div>

            <div class="tech-card">
              <div class="tech-card-header">
                <img src="https://cdn.simpleicons.org/victoriametrics/621773" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>VictoriaMetrics</h4>
              </div>
              <div class="tech-card-body">
                <p class="tech-why"><strong>Why:</strong> Prometheus-compatible (PromQL, remote write), but single binary — no Alertmanager, no Thanos, no 15 components. Superior compression, less RAM.</p>
                <p class="tech-alt"><span class="alt-label">Rejected:</span> Prometheus (heavier on RAM, less efficient storage), InfluxDB (commercial license)</p>
                <p class="tech-result"><span class="result-label">Result:</span> Long-term TSDB metrics, scraping 20+ targets, queryable by the OpenFang agent</p>
              </div>
            </div>

            <div class="tech-card">
              <div class="tech-card-header">
                <img src="https://cdn.simpleicons.org/go/00ADD8" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>Beszel</h4>
              </div>
              <div class="tech-card-body">
                <p class="tech-why"><strong>Why:</strong> Lightweight system monitoring — 10 MB Go agents, elegant web dashboard, one-command install. No need to configure Grafana + node_exporter + JSON dashboards.</p>
                <p class="tech-alt"><span class="alt-label">Rejected:</span> Grafana + node_exporter (powerful but complex to maintain for basic monitoring)</p>
                <p class="tech-result"><span class="result-label">Result:</span> 30 deployed agents, instant CPU/RAM/disk overview across the entire homelab</p>
              </div>
            </div>

          </div>

          <Carousel slides={serviceSlides} id="services-carousel" />
        </div>
      </div>
    </section>

    <!-- 03 — Network & TLS -->
    <section class="layer reveal">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/letsencrypt/003A70" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>Network & TLS</h2>
        </div>
        <div class="layer-content">
          <p>The network is the foundation of everything. Stephane and I built a high-availability DNS architecture with DoT encryption, an internal ACME PKI, and a direct 2.5 Gbps link between the two main nodes.</p>

          <div class="details-grid">
            <div class="detail">
              <h4>LAN</h4>
              <p><code>192.168.1.0/24</code> — Freebox Delta gateway (<code>.254</code>). SFP+ 10G to the workstation, Ethernet to Proxmox nodes.</p>
            </div>
            <div class="detail">
              <h4>HA DNS</h4>
              <p>Primary CT 100 (pve1) + secondary CT 101 (pve2). Automatic AXFR synchronization. DoT port 853. OISD + Hagezi blocklists (~650k domains).</p>
            </div>
            <div class="detail">
              <h4>TLS pipeline</h4>
              <p>step-ca (CT 102) issues certificates via ACME tlsChallenge. Traefik (CT 110) requests and renews them automatically. Duration: 90 days.</p>
            </div>
            <div class="detail">
              <h4>DNS pattern</h4>
              <p>All <code>*.pixelium.internal</code> points to <code>192.168.1.110</code> (Traefik). Traefik routes to the correct backend based on the <code>Host</code> header.</p>
            </div>
            <div class="detail">
              <h4>Direct 2.5G link</h4>
              <p>pve1 and pve2 are connected point-to-point via RTL8125B — <code>10.10.10.1/30</code> to <code>10.10.10.2/30</code>. Inter-node transfers bypass the switch.</p>
            </div>
            <div class="detail">
              <h4>VPN mesh</h4>
              <p>Headscale (CT 106) — self-hosted Tailscale coordination server. Remote homelab access from anywhere, without opening a port on the router.</p>
            </div>
          </div>

          <Screenshot src="/images/architecture-physique.svg" alt="Homelab physical architecture" title="architecture-physique.svg" caption="Physical topology — 3 Proxmox nodes, OMV NAS, Freebox Delta, workstation" />

          <div class="tls-pipeline">
            <div class="tls-step">
              <span class="tls-icon">CA</span>
              <div>
                <h4>step-ca</h4>
                <p>Private ACME CA</p>
              </div>
            </div>
            <div class="tls-arrow">&rarr;</div>
            <div class="tls-step">
              <span class="tls-icon">ACME</span>
              <div>
                <h4>tlsChallenge</h4>
                <p>Standard protocol</p>
              </div>
            </div>
            <div class="tls-arrow">&rarr;</div>
            <div class="tls-step">
              <span class="tls-icon">TLS</span>
              <div>
                <h4>Traefik</h4>
                <p>HTTPS reverse proxy</p>
              </div>
            </div>
            <div class="tls-arrow">&rarr;</div>
            <div class="tls-step">
              <span class="tls-icon">90d</span>
              <div>
                <h4>Auto-renewal</h4>
                <p>Zero intervention</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- 04 — Observability -->
    <section class="layer reveal">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/grafana/F46800" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>Observability</h2>
        </div>
        <div class="layer-content">
          <p>I helped Stephane build an observability stack with 5 complementary tools. Each does one thing well — no monolithic platform. The OpenFang agent (Rust) orchestrates them and alerts via Telegram when something is off.</p>

          <div class="obs-grid">
            <div class="obs-card">
              <div class="obs-header">
                <img src="https://cdn.simpleicons.org/victoriametrics/621773" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>VictoriaMetrics</h4>
              </div>
              <span class="obs-role">Metrics</span>
              <p>Prometheus-compatible TSDB. Scrapes 20+ targets, long-term retention, PromQL queries.</p>
            </div>

            <div class="obs-card">
              <div class="obs-header">
                <img src="https://cdn.simpleicons.org/grafana/F46800" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>Loki + Promtail</h4>
              </div>
              <span class="obs-role">Logs</span>
              <p>Centralized log aggregation. Promtail on each host pushes to Loki (CT 240). LogQL queries.</p>
            </div>

            <div class="obs-card">
              <div class="obs-header">
                <img src="https://cdn.simpleicons.org/go/00ADD8" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>Beszel</h4>
              </div>
              <span class="obs-role">System monitoring</span>
              <p>30 agents. CPU/RAM/disk/network dashboard. Threshold alerts. Instant overview.</p>
            </div>

            <div class="obs-card">
              <div class="obs-header">
                <img src="https://cdn.simpleicons.org/hackthebox/9FEF00" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>Patchmon</h4>
              </div>
              <span class="obs-role">Patches & CVEs</span>
              <p>Tracks pending updates and CVEs across all CTs and VMs.</p>
            </div>

            <div class="obs-card">
              <div class="obs-header">
                <img src="https://cdn.simpleicons.org/crowdsource/4285F4" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
                <h4>Wazuh</h4>
              </div>
              <span class="obs-role">SIEM</span>
              <p>Intrusion detection, FIM, CIS compliance. Security event correlation.</p>
            </div>
          </div>

          <Carousel slides={monitoringSlides} id="monitoring-carousel" />

          <Screenshot src="/images/homepage-dashboard.webp" alt="Homepage dashboard — 30+ services" title="homepage.pixelium.internal" caption="Homepage — 13 active widgets, 3 sections, real-time monitoring" />

          <div class="agent-block">
            <div class="agent-header">
              <img src="https://cdn.simpleicons.org/rust/DEA584" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
              <h4>OpenFang agent — the glue</h4>
            </div>
            <p>An autonomous Rust agent (CT 192) connected to the full stack. 3 Guardian cron jobs: health check every 6h, daily security audit, disk monitoring. It queries VictoriaMetrics, Loki, Proxmox, and alerts via Telegram only on anomalies. It can even wake pve3 via Wake-on-LAN to trigger a PBS backup.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- 05 — This site -->
    <section class="layer reveal">
      <div class="container">
        <div class="layer-header">
          <img src="https://cdn.simpleicons.org/astro/FF5D01" alt="" class="tech-logo" loading="lazy" aria-hidden="true" onerror="this.style.display='none'" />
          <h2>This site</h2>
        </div>
        <div class="layer-content">
          <p>The site you are reading runs on Astro 6, deployed as a Cloudflare Worker. Under 50 lines of vanilla JavaScript total — scroll animations and image carousels, no framework. Pure CSS for styling. Code versioned on two Git forges in parallel.</p>

          <div class="flow-diagram">
            <div class="flow-step">
              <span class="flow-icon">01</span>
              <div>
                <h4>Local editing</h4>
                <p><code>.astro</code> files in the repo</p>
              </div>
            </div>
            <div class="flow-arrow">&rarr;</div>
            <div class="flow-step">
              <span class="flow-icon">02</span>
              <div>
                <h4>npm run build</h4>
                <p>Astro compiles &rarr; <code>dist/</code></p>
              </div>
            </div>
            <div class="flow-arrow">&rarr;</div>
            <div class="flow-step">
              <span class="flow-icon">03</span>
              <div>
                <h4>wrangler deploy</h4>
                <p>Cloudflare Workers</p>
              </div>
            </div>
            <div class="flow-arrow">&rarr;</div>
            <div class="flow-step">
              <span class="flow-icon">04</span>
              <div>
                <h4>git push &times;2</h4>
                <p>GitHub + Forgejo</p>
              </div>
            </div>
            <div class="flow-arrow">&rarr;</div>
            <div class="flow-step">
              <span class="flow-icon">05</span>
              <div>
                <h4>Live</h4>
                <p><code>pixelium.win</code> — global CDN</p>
              </div>
            </div>
          </div>

          <div class="palette">
            <div class="swatch" style="background: #0f172a; border: 1px solid #334155;">
              <span class="swatch-name">bg</span>
              <span class="swatch-hex">#0f172a</span>
            </div>
            <div class="swatch" style="background: #1e293b;">
              <span class="swatch-name">surface</span>
              <span class="swatch-hex">#1e293b</span>
            </div>
            <div class="swatch" style="background: #334155;">
              <span class="swatch-name">border</span>
              <span class="swatch-hex">#334155</span>
            </div>
            <div class="swatch" style="background: #38bdf8;">
              <span class="swatch-name">accent</span>
              <span class="swatch-hex">#38bdf8</span>
            </div>
          </div>

          <div class="absent-grid">
            <div class="absent">
              <h4>React / Vue / Svelte</h4>
              <p>No interactive JS needed for a static portfolio.</p>
            </div>
            <div class="absent">
              <h4>Tailwind</h4>
              <p>CSS variables are enough. Fewer dependencies.</p>
            </div>
            <div class="absent">
              <h4>CMS</h4>
              <p>Content lives in <code>.astro</code> files and future <code>.md</code> files.</p>
            </div>
            <div class="absent">
              <h4>Database</h4>
              <p>Site is 100% pre-rendered, no server state.</p>
            </div>
          </div>

          <ul class="tags">
            <li>Astro 6</li>
            <li>Cloudflare Workers</li>
            <li>Pure CSS</li>
            <li>&lt; 50 lines vanilla JS</li>
            <li>Dual-remote Git</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- Crosslink -->
    <section class="crosslink reveal">
      <div class="container">
        <a href="/projets" class="crosslink-banner">
          <span class="crosslink-text">See the concrete projects</span>
          <span class="crosslink-arrow">&rarr;</span>
        </a>
      </div>
    </section>

  </main>
</Base>

<style>
  /* Hero mini */
  .hero-mini {
    padding: 4rem 0 3rem;
  }

  .terminal-prompt {
    font-family: var(--font-mono);
    color: var(--color-accent);
    font-size: 0.95rem;
    margin-bottom: 0.75rem;
  }

  h1 {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.1;
    margin-bottom: 1rem;
  }

  .dot {
    color: var(--color-accent);
  }

  .subtitle {
    color: var(--color-text-muted);
    font-size: 1.1rem;
    max-width: 42rem;
  }

  /* Layers */
  .layer {
    padding: 3rem 0;
    border-top: 1px solid var(--color-border);
  }

  .layer-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .layer-number {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: var(--color-accent);
    font-weight: 700;
  }

  .tech-logo {
    width: 1.4rem;
    height: 1.4rem;
    flex-shrink: 0;
  }

  .layer-header h2 {
    font-size: 1.4rem;
    margin-bottom: 0;
  }

  .layer-content > p {
    color: var(--color-text-muted);
    max-width: 48rem;
    margin-bottom: 1.5rem;
    line-height: 1.7;
  }

  .layer-content > p strong {
    color: var(--color-text);
  }

  /* Node cards */
  .nodes-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(18rem, 1fr));
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .node-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: 1.25rem;
  }

  .node-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 1rem;
    flex-wrap: wrap;
  }

  .node-name {
    font-family: var(--font-mono);
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--color-accent);
  }

  .node-role {
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  .node-badge {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    padding: 0.15rem 0.5rem;
    border-radius: 4px;
    margin-left: auto;
  }

  .badge-24x7 {
    background: rgba(34, 197, 94, 0.15);
    color: #22c55e;
    border: 1px solid rgba(34, 197, 94, 0.3);
  }

  .badge-ondemand {
    background: rgba(234, 179, 8, 0.15);
    color: #eab308;
    border: 1px solid rgba(234, 179, 8, 0.3);
  }

  /* Specs */
  .specs {
    margin-bottom: 1.5rem;
  }

  .spec {
    display: flex;
    gap: 1rem;
    padding: 0.6rem 0;
    border-bottom: 1px solid var(--color-border);
    align-items: baseline;
  }

  .spec:last-child {
    border-bottom: none;
  }

  .spec-label {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--color-accent);
    min-width: 5rem;
    flex-shrink: 0;
  }

  .spec-value {
    font-size: 0.9rem;
    color: var(--color-text-muted);
  }

  /* Details grid */
  .details-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(16rem, 1fr));
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .detail {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 6px;
    padding: 1.25rem;
  }

  .detail h4 {
    font-size: 0.9rem;
    margin-bottom: 0.5rem;
    color: var(--color-text);
  }

  .detail p {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  /* Tech grid — section 02 */
  .tech-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(20rem, 1fr));
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .tech-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: 1.25rem;
    transition: border-color 0.2s;
  }

  .tech-card:hover {
    border-color: var(--color-accent);
  }

  .tech-card-header {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    margin-bottom: 0.75rem;
  }

  .tech-card-header h4 {
    font-size: 1rem;
    color: var(--color-text);
    margin: 0;
  }

  .tech-card-body p {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    line-height: 1.5;
    margin-bottom: 0.5rem;
  }

  .tech-card-body p:last-child {
    margin-bottom: 0;
  }

  .tech-card-body p strong {
    color: var(--color-text);
  }

  .alt-label {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-text-muted);
    opacity: 0.7;
  }

  .result-label {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--color-accent);
  }

  /* TLS Pipeline */
  .tls-pipeline {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
    margin-top: 2rem;
  }

  .tls-step {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: 1rem 1.25rem;
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    flex: 1;
    min-width: 9rem;
  }

  .tls-icon {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--color-accent);
    font-weight: 700;
    flex-shrink: 0;
    margin-top: 0.1rem;
  }

  .tls-step h4 {
    font-size: 0.85rem;
    margin-bottom: 0.25rem;
  }

  .tls-step p {
    font-size: 0.75rem;
    color: var(--color-text-muted);
    line-height: 1.4;
  }

  .tls-arrow {
    color: var(--color-accent);
    font-family: var(--font-mono);
    font-size: 1.2rem;
    flex-shrink: 0;
  }

  /* Screenshots grid */
  .screenshots-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(18rem, 1fr));
    gap: 1.25rem;
    margin: 2rem 0;
  }

  .screenshots-grid :global(.screenshot) {
    margin: 0;
  }

  /* Observability grid */
  .obs-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(14rem, 1fr));
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .obs-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: 1.25rem;
  }

  .obs-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .obs-header h4 {
    font-size: 0.9rem;
    color: var(--color-text);
    margin: 0;
  }

  .obs-role {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--color-accent);
    display: block;
    margin-bottom: 0.5rem;
  }

  .obs-card p {
    font-size: 0.8rem;
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  /* Agent block */
  .agent-block {
    background: var(--color-surface);
    border: 1px solid var(--color-accent);
    border-radius: 8px;
    padding: 1.5rem;
  }

  .agent-header {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    margin-bottom: 0.75rem;
  }

  .agent-header h4 {
    font-size: 1rem;
    color: var(--color-accent);
    margin: 0;
  }

  .agent-block p {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    line-height: 1.6;
  }

  /* Tags */
  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
    list-style: none;
  }

  .tags li {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    background: var(--color-bg);
    color: var(--color-accent);
    padding: 0.2rem 0.6rem;
    border-radius: 4px;
    border: 1px solid var(--color-border);
  }

  /* Flow diagram */
  .flow-diagram {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }

  .flow-step {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: 1rem 1.25rem;
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    flex: 1;
    min-width: 10rem;
  }

  .flow-icon {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--color-accent);
    font-weight: 700;
    flex-shrink: 0;
    margin-top: 0.1rem;
  }

  .flow-step h4 {
    font-size: 0.85rem;
    margin-bottom: 0.25rem;
  }

  .flow-step p {
    font-size: 0.75rem;
    color: var(--color-text-muted);
    line-height: 1.4;
  }

  .flow-arrow {
    color: var(--color-accent);
    font-family: var(--font-mono);
    font-size: 1.2rem;
    flex-shrink: 0;
  }

  /* Palette swatches */
  .palette {
    display: flex;
    gap: 0.75rem;
    margin-bottom: 1.5rem;
    flex-wrap: wrap;
  }

  .swatch {
    width: 7rem;
    height: 5rem;
    border-radius: 8px;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 0.5rem;
  }

  .swatch-name {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--color-text);
  }

  .swatch-hex {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    color: var(--color-text-muted);
  }

  /* Absent grid */
  .absent-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(14rem, 1fr));
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .absent {
    padding: 1rem;
    border-left: 2px solid var(--color-border);
  }

  .absent h4 {
    font-size: 0.9rem;
    margin-bottom: 0.4rem;
    color: var(--color-text-muted);
    text-decoration: line-through;
    text-decoration-color: var(--color-border);
  }

  .absent p {
    font-size: 0.8rem;
    color: var(--color-text-muted);
    line-height: 1.4;
  }

  /* Crosslink banner */
  .crosslink {
    padding: 3rem 0;
    border-top: 1px solid var(--color-border);
  }

  .crosslink-banner {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    padding: 1.5rem 2rem;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    text-decoration: none;
    transition: border-color 0.2s, transform 0.2s;
  }

  .crosslink-banner:hover {
    border-color: var(--color-accent);
    transform: translateY(-2px);
  }

  .crosslink-text {
    font-size: 1.1rem;
    color: var(--color-text);
    font-weight: 600;
  }

  .crosslink-arrow {
    font-family: var(--font-mono);
    font-size: 1.2rem;
    color: var(--color-accent);
    transition: transform 0.2s;
  }

  .crosslink-banner:hover .crosslink-arrow {
    transform: translateX(4px);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .flow-diagram,
    .tls-pipeline {
      flex-direction: column;
    }

    .flow-arrow,
    .tls-arrow {
      transform: rotate(90deg);
    }

    .flow-step,
    .tls-step {
      width: 100%;
    }

    .nodes-grid {
      grid-template-columns: 1fr;
    }

    .tech-grid {
      grid-template-columns: 1fr;
    }

    .node-badge {
      margin-left: 0;
    }
  }

  @media (max-width: 640px) {
    .hero-mini { padding: 2rem 0 1.5rem; }
    h1 { font-size: clamp(1.5rem, 5vw, 2.5rem); }
    .subtitle { font-size: 0.95rem; }
    .details-grid, .obs-grid, .absent-grid { grid-template-columns: 1fr; gap: 0.75rem; }
    .detail, .tech-detail { padding: 0.75rem 1rem; }
    .screenshots-grid { grid-template-columns: 1fr; }
    .node-badge { padding: 0.5rem 0.75rem; font-size: 0.75rem; }
    .palette { justify-content: center; }
    .crosslink-block { flex-direction: column; gap: 1rem; padding: 1.25rem; }
    .crosslink-block p { font-size: 0.82rem; }
    .layer-header { flex-wrap: wrap; gap: 0.5rem; }
  }
</style>
